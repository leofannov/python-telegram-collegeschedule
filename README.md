# python-telegram-collegeschedule
Скрипт для бота в Телеграм на Python для рассылки расписания групп
Telegram Bot для расписания занятий

Умный Telegram бот для отслеживания расписания занятий с автоматическими уведомлениями об изменениями и ежедневной рассылкой.

🚀 Основные возможности

· 📅 Просмотр расписания на сегодня, завтра, неделю
· 🔔 Умные уведомления об изменениях в расписании
· 📧 Ежедневная рассылка расписания на следующий день
· 🔔 Расписание звонков для каждого дня
· 👥 Поддержка нескольких групп
· 🕐 Автоматическое обновление расписания
· 💾 Кэширование данных для быстрой работы

🛠 Установка и настройка

Требования

· Python 3.8+
· Telegram Bot Token
· Доступ к Excel файлу с расписанием

Установка зависимостей

```bash
pip install -r requirements.txt
```

Настройка конфигурации

1. Создайте файл config.py на основе примера ниже:

```python
# Конфигурация диапазонов ячеек для разных групп
RANGES = {
    'Д013П': {
        'even': {
            'Понедельник': {'schedule': 'BX97:CA110', 'pair_numbers': 'B97:B110', 'time': 'D97:D110'},
            'Вторник': {'schedule': 'BX111:CA124', 'pair_numbers': 'B111:B124', 'time': 'D111:D124'},
            'Среда': {'schedule': 'BX125:CA138', 'pair_numbers': 'B125:B138', 'time': 'D125:D138'},
            'Четверг': {'schedule': 'BX139:CA152', 'pair_numbers': 'B139:B152', 'time': 'D139:D152'},
            'Пятница': {'schedule': 'BX153:CA166', 'pair_numbers': 'B153:B166', 'time': 'D153:D166'},
            'Суббота': {'schedule': 'BX167:CA180', 'pair_numbers': 'B167:B180', 'time': 'D167:D180'}
        },
        'odd': {
            'Понедельник': {'schedule': 'BX10:CA23', 'pair_numbers': 'B10:B23', 'time': 'D10:D23'},
            'Вторник': {'schedule': 'BX24:CA37', 'pair_numbers': 'B24:B37', 'time': 'D24:D37'},
            'Среда': {'schedule': 'BX38:CA51', 'pair_numbers': 'B38:B51', 'time': 'D38:D51'},
            'Четверг': {'schedule': 'BX52:CA65', 'pair_numbers': 'B52:B65', 'time': 'D52:D65'},
            'Пятница': {'schedule': 'BX66:CA79', 'pair_numbers': 'B66:B79', 'time': 'D66:D79'},
            'Суббота': {'schedule': 'BX80:CA93', 'pair_numbers': 'B80:B93', 'time': 'D80:D93'}
        }
    }
    # Добавьте другие группы по аналогии
}

# Список доступных групп (берётся из ключей RANGES)
AVAILABLE_GROUPS = list(RANGES.keys())

# Расписание звонков (первая и вторая половины пар)
BELLS_SCHEDULE = {
    'Понедельник': [
        {'pair': '1 пара', 'first_half': '9:30-10:15', 'second_half': '10:20-11:05'},
        # ... остальные пары
    ],
    # ... остальные дни
}

# Настройки недели
WEEK_CONFIG = {
    'base_week_type': 'odd',  # Тип базовой недели
    'base_week_number': 36      # Номер недели в году для базовой недели
}

# Пути к файлам
EXCEL_FILE = 'cache/schedule.xlsx'
CACHE_FILE = 'cache/schedule_hash.cache'
LAST_UPDATE_FILE = 'cache/last_update.txt'

# Токен бота Telegram
TOKEN = "YOUR_BOT_TOKEN_HERE"

# Настройки для cron
CRON_CHECK_INTERVAL = 10  # Интервал проверки в минутах
```

1. Настройте URL для скачивания расписания в файле cron_download.py:

```python
def download_schedule():
    """Скачивание расписания и проверка изменений"""
    url = "https://your-university.ru/schedule/schedule.xlsx"  # ЗАМЕНИТЕ НА ВАШ URL
    
    try:
        # ... остальной код
```

1. Получите токен бота:
   · Напишите @BotFather в Telegram
   · Создайте нового бота командой /newbot
   · Скопируйте полученный токен в config.py

🏃 Запуск бота

Основной бот

```bash
python bot.py
```

Система автоматического обновления

```bash
# Обычная проверка и скачивание
python cron_download.py

# Только проверка изменений
python cron_download.py check

# Принудительная проверка
python cron_download.py force_check
```

📋 Команды бота

Основные команды

· /start - Начало работы с ботом
· /setgroup - Выбор группы
· /today - Расписание на сегодня
· /tomorrow - Расписание на завтра
· /week - Расписание на всю неделю
· /schedule - Выбор расписания по дням и неделям

Уведомления и рассылка

· /mailing - Управление ежедневной рассылкой
· /mailing_status - Статус рассылки
· /changes - Управление уведомлениями об изменениях
· /update_info - Информация об обновлениях

Дополнительные команды

· /bells - Расписание звонков
· /bells_today - Звонки на сегодня

🔧 Управление системой

Команды для cron_download.py

```bash
# Скачать расписание и проверить изменения
python cron_download.py

# Только проверка изменений
python cron_download.py check

# Принудительная проверка
python cron_download.py force_check

# Статистика уведомлений
python cron_download.py stats

# Тестовая отправка уведомления
python cron_download.py test

# Отладка конкретной группы
python cron_download.py debug ГРУППА

# Очистка кэша парсера
python cron_download.py clear_parser_cache

# Очистка всех кэшей
python cron_download.py clear_all_caches

# Помощь по командам
python cron_download.py help
```

Настройка cron для автоматического обновления

Добавьте в crontab для автоматической проверки расписания:

```bash
# Проверять каждые 2 часа с 8:00 до 20:00
0 8-20/2 * * * cd /path/to/bot && python cron_download.py

# Или проверять каждые 30 минут
*/30 * * * * cd /path/to/bot && python cron_download.py check
```

📁 Структура проекта

```
├── bot.py                 # Основной файл бота
├── config.py             # Конфигурация групп и настроек
├── cron_download.py      # Система автоматического обновления
├── schedule_parser.py    # Парсер Excel расписания
├── change_detector.py    # Детектор изменений
├── smart_change_detector.py # Умный детектор изменений
├── change_notifier.py    # Система уведомлений
├── mailing_manager.py    # Менеджер рассылки
├── group_manager.py      # Менеджер групп пользователей
├── check_all_groups.py   # Утилита проверки всех групп
├── cache/               # Папка для кэша и временных файлов
└── requirements.txt     # Зависимости Python
```

⚙️ Настройка диапазонов ячеек

Для настройки диапазонов ячеек в Excel файле:

1. Откройте файл расписания в Excel
2. Определите координаты для каждой группы и дня
3. Формат: {'schedule': 'BX97:CA110', 'pair_numbers': 'B97:B110', 'time': 'D97:D110'}
   · schedule - ячейки с названиями предметов
   · pair_numbers - номера пар
   · time - время пар

🔍 Отладка и устранение неисправностей

Проверка конфигурации

```bash
# Проверить все группы
python check_all_groups.py

# Отладить конкретную группу
python cron_download.py debug Д013П
```

Очистка кэша

```bash
# Очистить только кэш парсера (рекомендуется)
python cron_download.py clear_parser_cache

# Полная очистка всех кэшей (экстренный случай)
python cron_download.py clear_all_caches
```

Просмотр статистики

```bash
python cron_download.py stats
```

📝 Примечания

· Бот автоматически определяет чётность/нечётность недели
· Система использует "умный" детектор изменений, который отслеживает только значимые изменения
· Все данные кэшируются для быстрой работы
· При изменении расписания отправляются уведомления всем подписанным пользователям

🐛 Поддержка

При возникновении проблем:

1. Проверьте настройки URL в cron_download.py
2. Убедитесь, что токен бота указан правильно
3. Проверьте диапазоны ячеек в config.py
4. Используйте команды отладки для диагностики
