# python-telegram-collegeschedule

Умный Telegram бот для автоматического отслеживания и рассылки расписания занятий учебного заведения. Бот поддерживает множественные группы, умное обнаружение изменений в расписании и гибкую систему уведомлений.

## 🚀 Основные возможности

### 📅 Функционал для пользователей
- **Просмотр расписания** на сегодня, завтра и всю неделю
- **Выбор группы** из доступных в системе
- **Расписание звонков** для каждого дня недели
- **Автоматическая рассылка** расписания на следующий день
- **Умные уведомления** об изменениях в расписании
- **Контактная информация** учебного заведения

### ⚙️ Административные функции
- **Панель управления** с расширенной статистикой
- **Система анти-флуда** с настраиваемыми лимитами
- **Модерация пользователей** (бан/разбан)
- **Управление правами** администраторов
- **Детальная статистика** запросов и активности
- **Мониторинг системы** в реальном времени

### 🔧 Технические возможности
- **Умный детектор изменений** - отслеживает только значимые изменения
- **Кэширование данных** для быстрой работы
- **Автоматическое обновление** расписания по расписанию
- **Поддержка MySQL** для хранения данных
- **Гибкая конфигурация** групп и диапазонов ячеек

## 🛠 Установка и настройка

### Предварительные требования
- Python 3.8+
- MySQL Server 5.7+
- Telegram Bot Token

### 1. Клонирование репозитория
```bash
git clone https://github.com/your-username/telegram-schedule-bot.git
cd telegram-schedule-bot
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка базы данных
Создайте базу данных MySQL и настройте подключение в `database_config.py`:
```python
# database_config.py
DB_HOST = 'localhost'
DB_USER = 'your_username'
DB_PASSWORD = 'your_password'
DB_NAME = 'schedule_bot'
DB_PORT = 3306
```

### 4. Конфигурация бота
Настройте параметры в `config.py`:
```python
# config.py
TOKEN = "your_telegram_bot_token"
AVAILABLE_GROUPS = ['Д013П', 'Д014П', 'Д015П']  # Список групп
```

### 5. Настройка диапазонов ячеек
В `config.py` укажите диапазоны Excel для каждой группы:
```python
RANGES = {
    'Д013П': {
        'even': {
            'Понедельник': {'schedule': 'BX97:CA110', 'pair_numbers': 'B97:B110', 'time': 'D97:D110'},
            # ... остальные дни
        },
        # ... нечетная неделя
    }
}
```

## 🚀 Запуск

### Основной бот
```bash
python bot.py
```

### Автоматическое обновление расписания
```bash
python cron_download.py
```

### Принудительная проверка изменений
```bash
python cron_download.py force_check
```

## 📁 Структура проекта

```
telegram-schedule-bot/
├── bot.py                 # Основной файл бота
├── config.py             # Конфигурация приложения
├── database_config.py    # Настройки базы данных
├── database_manager.py   # Менеджер базы данных
├── schedule_parser.py    # Парсер Excel расписания
├── change_detector.py    # Детектор изменений
├── smart_change_detector.py # Умный детектор изменений
├── change_notifier.py    # Система уведомлений
├── mailing_manager.py    # Менеджер рассылки
├── group_manager.py      # Менеджер групп
├── flood_protection.py   # Защита от флуда
├── restart_service.py    # Перезагрузка службы
├── cron_download.py      # Автообновление расписания
├── check_all_groups.py   # Проверка всех групп
└── cache/               # Кэш-директория
```

## 🔧 Команды бота

### Основные команды
- `/start` - начать работу с ботом
- `/setgroup` - выбрать группу
- `/today` - расписание на сегодня
- `/tomorrow` - расписание на завтра  
- `/week` - расписание на неделю
- `/bells` - расписание звонков
- `/mailing` - управление рассылкой
- `/changes` - уведомления об изменениях
- `/contact` - контактная информация

### Административные команды
- `/service_help` - справка по админ-командам
- `/setadmin` - назначить администратора
- `/stats` - статистика запросов
- `/sysinfo` - системная информация
- `/ban` - заблокировать пользователя
- `/reboot` - перезагрузить бота

## ⚙️ Конфигурация Cron

Для автоматического обновления расписания настройте cron:

```bash
# Ежечасная проверка обновлений
0 * * * * cd /path/to/bot && python cron_download.py

# Ежедневная принудительная проверка в 8:00
0 8 * * * cd /path/to/bot && python cron_download.py force_check
```

## 🛡 Система безопасности

### Анти-флуд защита
- Настраиваемые лимиты запросов в минуту
- Временная блокировка при превышении лимитов
- Детальная статистика запросов

### Модерация
- Система банов с указанием причины и срока
- Управление правами администраторов
- Логирование всех действий

## 📊 Мониторинг

### Статистика
- Количество активных пользователей
- Статистика запросов по командам
- Мониторинг использования ресурсов
- Отслеживание изменений расписания

### Системная информация
- Использование CPU и памяти
- Статус базы данных
- Время работы системы
- Сетевые метрики

## 🔄 Умный детектор изменений

Бот использует интеллектуальную систему обнаружения изменений, которая:
- Отслеживает только значимые изменения в расписании
- Игнорирует несущественные правки форматирования
- Сравнивает хэши данных для эффективности
- Отправляет уведомления только при реальных изменениях

## 🤝 Разработка

### Добавление новой группы
1. Добавьте группу в `AVAILABLE_GROUPS` в `config.py`
2. Настройте диапазоны ячеек в `RANGES`
3. Протестируйте парсинг для новой группы

### Расширение функционала
- Новые команды добавляются в `bot.py`
- Логика парсинга - в `schedule_parser.py`
- Работа с БД - через `database_manager.py`

## 📝 Лицензия

Этот проект лицензирован под MIT License - смотрите файл LICENSE для деталей.

## 📞 Поддержка

При возникновении проблем или вопросов:
1. Проверьте логи в директории `cache/`
2. Убедитесь в корректности настроек БД
3. Проверьте доступность файла расписания
4. Создайте issue в репозитории

---

**Разработано для автоматизации учебного процесса** 🎓
